import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  listContent: {
    paddingBottom: 20,
  },
  footer: {
    paddingVertical: 20,
    alignItems: 'center',
  },
  emptyContainer: {
    flex: 1,
  },
});
