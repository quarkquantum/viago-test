# ERD Viewer

This application is used to generate and view the Entity-Relationship Diagram (ERD) for the database schema.

## Usage

To generate and view the ERD, run the following command:

```bash
pnpm dev
```

This will build the ERD from the Prisma schema file and serve it on a local webserver.
